part of remaths;

Node withRepeat(Node animation, [int numberOfReps = 2, bool reverse = false]) {
  return (node) {
    animationLoop(int number) {
      UnimplementedError();
    }

    ;

    animationLoop(numberOfReps);
  };
}
